A. render
B. view
C. container
	a. subscribe
	b. createStore	
		1. getState 	// returns current app state
		2. subscribe	// registres a callback to be called when the app state changes
		3. dispatch	// sends an action to be applied to the current state
D. update

--STOPWATCH EXAMPLE--

create update

create view

create model

create createStore

create container

create render

container -> subscribe(render)
// state created filled with the root DOM

view(<current state>);
	view -> print HTML
		create dispatch

createStore.dispatch -> 





